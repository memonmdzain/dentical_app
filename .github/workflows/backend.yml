name: Backend CI

on:
  push:
    branches:
      - develop
      - main
    paths:
      - 'backend/**'
  pull_request:
    branches:
      - develop
      - main
    paths:
      - 'backend/**'

jobs:
  placeholder:
    name: Backend CI (Placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Backend CI will be configured when backend module is set up."
